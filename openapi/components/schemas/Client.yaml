title: Client
required:
  - name
type: object
properties:
  client_id:
    type: integer
    description: The ID of this client. Read-only
    example: 1
  name:
    maxLength: 200
    type: string
    description: The name of the client
    example: Red Circle
